package com.ruslan.mentoring.jpa.services;

import javax.persistence.EntityManager;
import javax.persistence.EntityManagerFactory;
import javax.persistence.Persistence;

abstract class AbstractService {
    protected static final String MESSAGE_KEY = "message";

//    private EntityManagerFactory entityManagerFactory = Persistence.createEntityManagerFactory("com.ruslan.mentoring.jpa");
//    protected EntityManager entityManager = entityManagerFactory.createEntityManager();

//    @RequestMapping(method = RequestMethod.GET, value = "{id}")
//    public String getEmployee(Map<String, Object> model, @PathVariable Long id) {
//
//    }
}
