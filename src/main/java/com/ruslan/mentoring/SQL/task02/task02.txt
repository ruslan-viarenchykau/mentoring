Write Highload Writing Console Tool which will write to database (URL/Name is command line parameter)

Creates N random tables  with random name (or names from dictionary) and K random columns and random types
Creates M random rows for each table
It supports L parallel threads for data generation or generate special warnings if several parameters are incoorect
Tune performance and provide metrics for different cases and performance improvments

N,M,K,L should be command line parameters

10 GB sample database should be generated easy